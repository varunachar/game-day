/**
 * Encapsulate all utils
 */
var Utils = {  
    /**
     * 
     * @param {String} val 
     */
    isEmpty : function(val) {
        return val === undefined || val.length == 0 ? true : false;
    }

};